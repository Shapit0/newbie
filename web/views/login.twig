<!DOCTYPE html>
<html lang="en">
	<head>
		
	</head>
	<body>
	<h1>Авторизуйся!</h1>
	{{ error }}
	<form action="{{ path('admin_login_check') }}" method="post">
	    <input type="text" name="_username" value="{{ last_username }}" />
	    <input type="password" name="_password" value="" />
	    <input type="submit" />
	</form>
	<p>Регистрация <a href="{{ app.url_generator.generate('registration') }}">тут</a></p>
	</body>
</html>